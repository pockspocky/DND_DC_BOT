version: '3.8'

services:
  dnd-bot:
    build: .
    container_name: dnd-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./dnd_bot.db:/app/dnd_bot.db
      - ./logs:/app/logs
